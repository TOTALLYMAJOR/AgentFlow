# AgentFlow examples

Copy `.agentflow.yaml` and `BACKLOG.md` into a disposable Git repository, then
adapt names, ownership roots, validation commands, and artifacts to that
repository. Review every command before running a build.
